# v15 — ADAPT: retain narrow BF16 draft control and independent references

Assigned `2e010408b9713dca3de690f339ef46fbe72f090f`, parent
`32289022a4e362b506a4de1f89e1abe2957660c1`; recomputed stable patch ID
`916682cf96752788c985d07011f2657c54dfc44d`. Targets: vLLM v0.30.0
`ced6857afa0ea7b2e3f0846a62e1394e90f15607`, kernels v0.1.15
`1c7cbeee1cd0c1481d48f5031f679a47f3f0ef45`. Old bases, frozen main/release
tips and full companion inventory are recorded in [manifest](../manifest.json)
and [commit map](../commit-map.json). Inspected both source AGENTS, audit
contract, immutable implementations and current v05/v09 reports. Verified
prior v15 report SHA256:
`05e106b911bc12e4a4b59be697c089d850668bbb92d255233cacc6d3e8d082a1`.
Only this report was written; no source/ref/lock/service changes, builds or
GPU execution. This is the ordinary eager V1 release; issue17 stays paused.

**Requirement and residual.** Preserve explicit `bfloat16` draft storage for
the existing bundled Qwen MTP configuration with compact K4V2 target, while
preserving independently selected backend/configuration and physical pools.
The B70 contract uses BF16 compute, TP1/PP1, one/two speculative tokens,
at most 2,048 prefill tokens, eager V1 and existing Qwen vision restrictions.
v15 adds only a narrow admission branch and diagnostic text to production
`vllm/platforms/xpu.py:_check_kvarn_beta_unsupported_config`. K4V4-target/BF16
draft and `auto`/`bf16` aliases remain rejected. The parent rejects BF16;
the assigned and final release implementations admit it. All five touched
files have empty v15-to-released-tip diffs, so v16 does not supersede v15.

**Actual release coverage.** Neither selected target contains KVarN or this
admission rule. From old vLLM base
`51da0ca66c8065619c79e35dff97aa99aeaf5644` to the selected release, the only
XPU-platform change is unrelated sparse-GELU selection
`78e84261ab2ffb691a8079d698d1f3f96ecb7635`. Generic V1 isolation already
exists: `fd74c90d9c3b5c35308f1f0ab308469235fa5277` and
`8bfd6839016f9670508cf07c16c68891f83ea117` are target ancestors.
`SpecDecodeBaseProposer._create_draft_vllm_config`, lines 1273–1309, replaces
the attention configuration and explicit cache dtype independently, without
mutating the target. Reuse that implementation. XPU's selector defaults
ordinary BF16 to FlashAttention; the retained KVarN feature supplies compact
dispatch. Configuration copying does not prove mutable-builder ownership.

The selected kernel's `flash_attn_interface.py`, lines 620–653, still catches
native `not compiled` errors and calls `_fallback_varlen_attn`. The wrapper
and attention sources have no diff from the prior snapshot kernel target
`da16a5595c105605bcd44b558c7933b934a05f85`. Thus prior fallback concerns
still apply to this release. Native `DISPATCH_BY_KV_CACHE_DTYPE` still
rejects literal `bfloat16`; v09's guarded translation to `auto` remains
required, as confirmed by its new target-specific audit. No new ABI is needed.

**Disposition: ADAPT**, retaining the capability and tests through two folds:

- Fold admission, unchanged-target/independent-draft configuration tests and
  heterogeneous-pool controls into v05
  `fcab69494f1719e91e416aace17ef6ad501fe900`. Preserve v10 compact plumbing
  `80d5166fcb0b7935f3f95b8d343e3e669940261f`, v11 exclusive draft metadata
  ownership `73fb9e7aa230e9d6164c810a05175bd342995565`, v12 whole compatible
  groups `6519bf709dbb354926f1ac3908e2c7917e9abd23`, and v13 recurrent
  reservations `d6cb2e6cb2de2321fd28eea6e0aada93fb8bd736`. BF16 pool cells
  use ordinary `FullAttentionSpec`; they assert groups `[1,16,48]`, equal
  capacity/bytes across grouping strategies and recurrent slots across
  overrides. They do not establish pointer separation or runtime ownership.
- Fold `test_bf16_draft_paged_attention_matches_independent_causal_reference`
  into v09 `74a764d57a8c3c2e6bc1376be0f9dfa122aed90f`, preserving attribution.
  Twelve cells cover B1/4, Q1/3/129, D256 Q24/KV4, sparse physical pages,
  padding 0/123 and lengths 257–641. FP32 SDPA with an explicit bottom-right
  causal mask supplies expected results; final-visible-value poisoning and
  restoration check causal visibility and exact replay. Monkeypatch
  `_fallback_varlen_attn` to raise and include a missing-policy sensitivity
  control. These tests do not establish long-context behavior or scheduler
  rejection/rollback.

K4V2 storage still requires paired k08/k09
`9bb1e2d428b8a25b262c0cd6704614bdf6ee3ad8` /
`f26ef2323399ab410299db5c8caec41c8d63215a`; v15 adds no operator. Preserve
V1 ownership/guard coverage during v05 replay. No V2/DFlash port, new dtype
alias, changed default or BF16 serving milestone belongs to this change.

**Validation observed.** Through clone `.venv/bin/python -B`, standard-library
AST extraction executed 45 guard cases against parent/assigned/released
functions, three actual-target V1 config-copy cases, and the selected
kernel's real missing-policy exception arm with a forbidden-fallback sentinel:
all passed. These use configuration/operator stubs without importing Torch
or initializing XPU. Assigned/released guard AST SHA256 matches:
`9b175f1ac3f27723916d3dfbc830fb2fd3a8ef83b1b3cfe8bf8cd4d4d8ee1ef7`.
Pool tests were inspected, not executed against a replayed candidate.

Re-read retained `benchmark-results/kvarn-mtp-draft-cache/20260914T232612Z/metadata-repair-v1/`
`cpu-installed-outcomes.json` and `gpu-installed-outcomes.json`: return code
zero, respectively 122 and 62 listed passes, including the relevant pool/config
cells and all twelve finite-padding references. These historical runs do not
qualify the new pair. Prior evidence retains six NaN-padding diagnostic failures
and arithmetic 901-versus-899 across BF16/compact/MTP-off controls; v15 does
not resolve them or justify a serving/performance claim.

Pending: final-pair admission/configuration/pool and exclusive-owner tests;
installed native writer/reference cells with fallback forbidden; then ordinary
V1 K4V2 target/draft MTP2, vision, capacity and lifecycle qualification.
