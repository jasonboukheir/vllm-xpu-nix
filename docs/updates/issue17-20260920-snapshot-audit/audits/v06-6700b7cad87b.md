# v06 — retirement of the model-inspection hard exit

**Recommendation: DROP `6700b7cad87b884a49ce2e5e6c33c86f1b5529b3` together with v01 `f1056606736e309b4bf4e912a353ac9d75340197`. Minimum residual source change: none.** Preserve normal inspection-child teardown, failure propagation and the fixed Torch dependency. This independently agrees with the [v01 audit](v01-f1056606736e.md); dropping the pair does not retire the underlying startup/lifecycle requirement.

Auditor: `/root/audit_v06`, 2026-09-20 UTC. Read both source `AGENTS.md` files, the commit-audit contract, and this run's manifest, issue snapshot and complete paired commit map. Scope: read-only inspection and CPU-only extracted-function checks. Only this report was written; no source/ref, GPU, service or publication changes occurred.

| Frozen input | Value |
| --- | --- |
| Repository | `/home/jasonbk/Projects/vllm-xpu-nix/build-dev/issue17/vllm` |
| Commit / parent | `6700b7cad87b884a49ce2e5e6c33c86f1b5529b3` / `fcab69494f1719e91e416aace17ef6ad501fe900` |
| Subject / stable patch ID | `fix(xpu): retire the model-inspection hard exit` / `1486b039eaf4a7b8c7024972456261db9e7a5b0f` |
| Old base / main | `51da0ca66c8065619c79e35dff97aa99aeaf5644` / `3b6c822e8a4f56dc3904c7fce388a76dff4c7bdd` |
| Full released tip / target snapshot | `2fdd9d71f6895379b0940a8f755b4c9095840558` / `e378275a8f20eac9e92212e4a1fee84ec5a31dc7` |
| Companion repository | `/home/jasonbk/Projects/vllm-xpu-nix/build-dev/issue17/vllm-xpu-kernels` |
| Companion old base / main | `e7f20cbc18d741419a84aa57178e1f1cc0b453da` / `9bb1e2d428b8a25b262c0cd6704614bdf6ee3ad8` |
| Companion released tip / target | `2c39287deec5e59cc2656e6d4a6dce51a567c411` / `da16a5595c105605bcd44b558c7933b934a05f85` |
| Paired inventory | [commit-map.json](../commit-map.json): v01–v16 and k01–k11, including release-only fixes |

The original failure was successful model inspection followed by an XPU/native teardown crash. Torch's finalizer could resolve an uncached operator after native state destruction, producing invalid overload names, Unicode failures or SIGSEGV. The parent treated that nonzero child exit as failed inspection despite a written result. v01 bypassed all interpreter teardown after serialization using an XPU-only `os._exit(0)`. v06 removes that workaround after the dependency fix. This concerns cold model discovery on Brutus and remains relevant to target/draft imports, independently of runner, quantization or cache choice. It is not a decode performance feature.

Exact cancellation was independently established with `git show`, `git rev-parse <sha>:vllm/model_executor/models/registry.py`, path-filtered `git log`, and `git patch-id --stable`. Parent and patch ID match the table. v06 deletes exactly the 14 lines v01 introduced. Old base, v06, old main and full released tip all resolve registry.py to blob **`aa1e6137658d297ae2c87cbe3f8ddb9d1aef70cc`**. Between v01 and the full release, v06 is the only registry-path commit. Intervening v02–v05 implement GDN/RMSNorm/KVarN behavior; their changed-path inventory and inspected `_xpu_ops.py`/`platforms/xpu.py` deltas introduce no use of the exit workaround. The bypass exported no API, schema or persistent state: it executed only after the child wrote its result. No companion kernel deletion or fold is required.

At frozen target `e378275a8f20eac9e92212e4a1fee84ec5a31dc7`, `registry.py::_run` loads plugins, deserializes and executes the callback, writes the pickle and returns normally. `_run_in_subprocess` checks the subprocess return code before opening its result and includes stderr in raised failures. AST comparisons proved both functions identical across old base, full released tip and target. The six upstream registry commits from `fe26c705f7099da3b155b3cb6218f013bb308e63` through `8c1557a79c539ffe82d004d2a0c8d7b5e71159ce` change model registrations, typing and docstrings, not this lifecycle. There is no newly added vLLM workaround to credit as coverage.

The underlying replacement is [PyTorch commit `c5f8ebc91a8727a9056734f73329c217328b8989`](https://github.com/pytorch/pytorch/commit/c5f8ebc91a8727a9056734f73329c217328b8989), inspected directly. `_clear_torch_ops_cache` checks the namespace dictionary instead of probing an uncached operator through `__getattr__`; its accompanying regression checks that unsafe lookup is avoided. The installed source independently confirms this implementation: `/nix/store/95g5rvddxh9ai7k3pyhdjh4ar819w0fz-python3.12-torch-2.13.0+xpu/lib/python3.12/site-packages/torch/library.py:613`, SHA-256 **`82fef5b20f95d42a4494b957086181147f521e922d688895a1041e29e8364ee5`**. `_del_library` resets native state then calls this helper. Adjacent `version.py` identifies `2.13.0+xpu`, Git `cf30153c4c131c8164ee7798e5022d810682e2cb`. Packaging `nix/torch-xpu.nix:60` pins that wheel with `sha256-njm89P85dNfX0fpOJOOuTkiV2pfYdkejDG8WG5E23fw=`; target `requirements/xpu.txt:17` requires Torch 2.13.0.

Observed validation used `/home/jasonbk/Projects/vllm/.venv/bin/python -B` with `PYTHONDONTWRITEBYTECODE=1`, extracting functions through AST without importing Torch or touching devices/files:

- Inspection child/parent AST equality across the three immutable sources passed.
- The target parent function, supplied a failed `CompletedProcess` and an available in-memory result, preserved stderr and raised before reading output; its successful-child control returned the complete pickle.
- Installed Torch's helper removed a cached overloaded operator and its directory entry without looking up an uncached operator. Replacing the guard with pre-fix `hasattr` triggered the instrumented unsafe-lookup exception. Both behavior and sensitivity checks passed.

Historical evidence is narrower than new runtime qualification. v06's commit message records nine cold inspections, four real failure paths and cold 27B health/generation. `docs/releases/xpu-v1.8.0.md` records the released retirement with unchanged Torch. The retained issue6 snapshot documents the dependency rationale. Dedicated retirement raw logs were not located in the artifact-name search; those counts are recorded claims, not independently reproduced results. The CPU checks prove control flow and the Python guard, not native driver shutdown safety.

Parent reconciliation should map both commits to paired DROP and check that replay never reintroduces the hard exit. Retain this fixed dependency or verify its replacement. Final-package qualification remains pending: fresh-cache target and pinned syvai inspections, real import/callback/write failures, complete readable results, cold V2 startup, orderly shutdown/restart, and MTP/vision lifecycle coverage. Record exact runtime/native identities and any teardown stderr/backtrace. These gates cover new-stack interactions; no throughput comparison is needed for a net-zero inspection workaround.
