# v01 — model-inspection hard exit

**Recommendation: DROP `f1056606736e309b4bf4e912a353ac9d75340197` together with v06, `6700b7cad87b884a49ce2e5e6c33c86f1b5529b3`. Minimum residual source delta: none.** Their registry changes cancel exactly in both rolling main and the released source tip. The pinned Torch artifact contains the underlying finalizer repair. Preserve normal child teardown and genuine inspection-failure propagation, then requalify cold inspection in the final package.

Auditor: `/root/r30_v01`; inspected 2026-09-23 UTC. This is a new target-specific audit for the v0.30 release workflow, preserving released eager V1 K4V2 MTP and vision. Issue #17's V2/DFlash work remains paused and is not a qualification requirement for this release. Read `.agents/skills/vllm-xpu-update/references/commit-audit.md` and the source `AGENTS.md`. Work was read-only except this report; no source, refs, locks, services, builds or GPU operations were changed/run.

## Frozen identities

| Input | Identity |
| --- | --- |
| Commit / parent | `f1056606736e309b4bf4e912a353ac9d75340197` / `51da0ca66c8065619c79e35dff97aa99aeaf5644` |
| Subject / patch ID | `fix(xpu): avoid driver teardown crashes after model inspection` / `e78d3b1d691e22993c1df45df4a52fd0fd14c743` |
| vLLM old base | `51da0ca66c8065619c79e35dff97aa99aeaf5644` |
| vLLM rolling main / released tip | `3b6c822e8a4f56dc3904c7fce388a76dff4c7bdd` / `2fdd9d71f6895379b0940a8f755b4c9095840558` |
| vLLM selected release | `v0.30.0`, `ced6857afa0ea7b2e3f0846a62e1394e90f15607` |
| Kernels old base | `e7f20cbc18d741419a84aa57178e1f1cc0b453da` |
| Kernels rolling main / released tip | `9bb1e2d428b8a25b262c0cd6704614bdf6ee3ad8` / `2c39287deec5e59cc2656e6d4a6dce51a567c411` |
| Kernels selected release | `v0.1.15`, `1c7cbeee1cd0c1481d48f5031f679a47f3f0ef45` |

Inspected frozen clones under `build-dev/release-v030-20260922/`. Full inventories are the run's `commit-map.json`, v01–v16 and k01–k11, including release-only repairs. The prior individual audit at `docs/updates/issue17-20260920-snapshot-audit/audits/v01-f1056606736e.md` was read as historical evidence, not adopted without checking the new target.

## Requirement and old-stack contribution

The original workaround added 14 lines to `vllm/model_executor/models/registry.py::_run`: after successfully serializing the inspection result, an XPU child flushed stdout/stderr and called `os._exit(0)`. The recorded failure was an uncached Torch operator lookup during interpreter/native teardown, causing garbage overload names, Unicode failures or SIGSEGV. A nonzero child status invalidates inspection even when its result file was already written. This concerns cold startup and lifecycle correctness on Brutus, independent of KV dtype or speculative method; it is not a token-generation optimization.

v06 removes those exact 14 lines. Fresh immutable-object checks returned registry blob `aa1e6137658d297ae2c87cbe3f8ddb9d1aef70cc` at old upstream base, v06, rolling main and released tip. The base-to-release registry diff is empty. Path history from v01 through v06 contains only v06 after the initial workaround. The intervening v02/v03/v04/v05 changed-path inventory contains no registry edit; their XPU dispatch/platform additions contain no subprocess, hard-exit, finalizer or inspection references. The bypass introduced no callable interface consumed by those features. Their final released behavior already includes v06's normal teardown.

v06's commit records nine cold inspections, four real subprocess failure paths, and cold 27B health/generation success on Brutus. `docs/releases/xpu-v1.8.0.md:23` records the retirement in a release. These are retained historical statements, not raw runtime results independently rerun by this audit; the prior audit did not locate the dedicated nine/four raw traces. Exact cancellation is independently verified here regardless of that historical limitation.

## Upstream and dependency coverage

At the selected v0.30.0 target, `_run_in_subprocess` at `registry.py:1536` launches the child, checks its return code at line 1555, and only then reads the result at line 1563. `_run` at line 1566 loads plugins, executes the callback, writes its result and returns normally. It has no equivalent `os._exit` bypass. Thus real callback/import/write failures continue to propagate; a successful result file cannot hide a nonzero child exit.

Fresh old-base versus selected-target inspection shows the entire suffix from `def _run_in_subprocess` through EOF is byte-identical: SHA-256 `b243a64287710fe86e7a7cf6d062b901df7e270d57eedba17d9635223f240ecd` at both refs. Old-to-v0.30 registry history contains these unrelated model/typing changes:

- `fe26c705f7099da3b155b3cb6218f013bb308e63` — Bailing V3 VL support.
- `f9083cb83af44debe290f3753ca2effc292ea1c1` — model typing.
- `e77daef89e18e08321ae7b8b24827eedd5fe8673` — DeepSeek V4.1 support.
- `cf1584f373d06f03c5662608fb80cf66c03fcb1d` — DeepSeek package naming.
- `b3124a8237f21fcd3a4510002a5e2925b0dbedfe` — Nanbeige support.

The underlying coverage comes from Torch, not a replacement vLLM bypass. The prior audit and v06 identify PyTorch `c5f8ebc91a8727a9056734f73329c217328b8989` / #186611. I directly rechecked the actual installed artifact rather than relying on its version label:

- `nix/torch-xpu.nix:48` pins `2.13.0+xpu`, wheel hash `sha256-njm89P85dNfX0fpOJOOuTkiV2pfYdkejDG8WG5E23fw=`.
- Selected vLLM `requirements/xpu.txt:17` requires `torch==2.13.0`; selected kernels `requirements.txt:18` requires `torch==2.13.0+xpu`.
- Artifact `/nix/store/95g5rvddxh9ai7k3pyhdjh4ar819w0fz-python3.12-torch-2.13.0+xpu/lib/python3.12/site-packages/torch/library.py` has SHA-256 `82fef5b20f95d42a4494b957086181147f521e922d688895a1041e29e8364ee5`.
- `_clear_torch_ops_cache` at lines 613–636 guards uncached names with `if name not in vars(namespace): continue`, avoiding `_OpNamespace.__getattr__` and its C++ lookup after native destruction. `_del_library` calls this cleanup after `m.reset()` at lines 665–667.
- `torch/version.py` records `2.13.0+xpu`, source `cf30153c4c131c8164ee7798e5022d810682e2cb`, XPU `20260000`.

This fixes the specific unsafe lookup without skipping all interpreter destructors. It does not establish freedom from every possible native shutdown bug. A deliberately changed Torch artifact must independently retain equivalent behavior.

## Dependencies and minimum replay

- Pair v01 with v06 as a net-zero DROP; reconcile with v06's independent audit before replay. Do not replay v01 just to delete it later.
- No source change, operator removal, cross-repository API/ABI change or kernel fold results from this pair.
- Keep the fixed Torch behavior and the release's inspection/lifecycle gates. No extra permanent source patch is justified merely to carry those qualification checks.
- After reconstruction, inspect the effective registry diff and ensure the XPU `os._exit(0)` workaround did not reappear. Recovery history retains both original commits and authorship.

## Checks actually run and remaining gates

Run during this audit:

1. `git show` of v01/v06; `git rev-parse <ref>:vllm/model_executor/models/registry.py` at the four refs above; empty base-to-released registry diff; registry path history and intervening changed-path inventory. All supported the exact cancellation.
2. `git log <old-base>..<target> -- registry.py`, current source-line inspection, and `git show <ref>:registry.py | sed -n '/^def _run_in_subprocess/,$p' | sha256sum` at old/target. Both suffix hashes match; failure propagation remains intact.
3. Read/hash the installed Torch finalizer and exact version file; inspect both selected source dependency requirements. Results are recorded above.
4. CPU-only extracted-function check with `PYTHONDONTWRITEBYTECODE=1 /home/jasonbk/Projects/vllm/.venv/bin/python -B`. AST-extracted `_clear_torch_ops_cache` ran against a fake namespace whose `__getattr__` raises `RuntimeError('unsafe C++ lookup')`. Clearing one uncached op and one cached overload made zero native lookups, removed the cached attribute and its `_dir` entry: **PASS**. Replacing the instance-dictionary guard with the former `hasattr(namespace, name)` triggered the expected exception: **PASS sensitivity control**. No Torch import, device access, source/cache writes or native driver execution occurred.

The final `rg` search for newly added teardown/inspection references returned exit 1 because there were no matches; this was not a failed regression. No package build, pytest suite, GPU runtime check or performance measurement was run.

Still required in parent-owned final-package qualification, under the separate GPU-maintenance authorization:

- Fresh-cache cold inspection for the deployed Qwen target and bundled MTP/vision architecture paths; require complete results, normal successful-child exit, no finalizer traceback/core dump.
- Real import/callback/result-write failures must remain failures with useful stderr, including rejecting a result pickle after nonzero child status.
- Cold eager V1 K4V2 target/draft MTP startup and orderly shutdown/restart on Brutus; retain the release's vision regression coverage. Record final package, Torch/native dependencies and device identity.
- If teardown fails, retain raw stderr/backtrace and diagnose its owner; do not silently restore the hard exit.

No throughput comparison is required for this removed startup workaround, which contributes no effective code to the released baseline. DFlash2, V2 and their performance targets are outside this release audit.
