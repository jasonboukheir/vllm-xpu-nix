# k01 — deterministic oneDNN W4A16 matmul

**Disposition: KEEP.** Preserve the three-line deterministic-attribute addition and its focused regression file as one commit. The released upstream targets still use the same oneDNN implementation and dependency revision; neither supplies equivalent coverage. This preserves reproducibility, with no claimed speed improvement.

Assignment: `8530de9d4298b199436079c46233acb33efb6fc2`, parent/old kernel base `e7f20cbc18d741419a84aa57178e1f1cc0b453da`, stable patch ID `e97db47d147665e6027a87a3b8cdd21c5d13e4fc` (recomputed). Inputs and full v01–v16/k01–k11 inventory are in [commit-map.json](../commit-map.json) and [manifest.json](../manifest.json). The kernel target is v0.1.15 `1c7cbeee1cd0c1481d48f5031f679a47f3f0ef45`; companion vLLM is v0.30.0 `ced6857afa0ea7b2e3f0846a62e1394e90f15607`. Inspected clones are the manifest's `build-dev/release-v030-20260922` repositories. Read both source `AGENTS.md` files and the commit-audit contract. The [prior audit](../../issue17-20260920-snapshot-audit/audits/k01-8530de9d4298.md) SHA-256 matches `954d43fd79c3a42b5f8343914156f6c17898d1559b0c4bfabb1d735ea1cd1918`; its snapshot target is superseded for this assessment.

The requirement is repeatable fixed-shape BF16 W4A16 projections on Brutus/Arc Pro B70, including the retained dense RedHat Qwen target. This ordinary release preserves eager V1, K4V2 target/draft MTP2 and vision. Issue17 remains paused; no DFlash/V2 work is required by this disposition. The patch requests `pattr.set_deterministic(true)` in `oneDNN::dnnl_matmul_w4a16_int4` to avoid variable floating-point global K-parallel accumulation. It also affects existing FP16 and asymmetric-zero-point callers, without changing their interface.

Nine regression cases require bitwise replay for production BF16/group-128/symmetric INT4 shapes, interleaving different tensor handles and allocator pressure. Six batch-separability cases compare M=4 against four M=1 calls with `atol=rtol=0.015625`, recording exactness separately. Fixed-shape repeatability does not promise bitwise equality across M, full-model determinism, or reference accuracy. Both patch files are unchanged from k01 through old main `9bb1e2d428b8a25b262c0cd6704614bdf6ee3ad8` and released tip `2c39287deec5e59cc2656e6d4a6dce51a567c411`; no follow-up repair or revert supersedes this contribution.

Upstream evidence at the frozen targets:

- Old and new kernel `csrc/xpu/onednn` resolve to identical tree `6907657b9b9a8ad655e5ddc0fee565ea636596aa`; the complete directory diff is empty. Header history still ends at `7671b1ca729f9c2b292aca6736b723118a769923` (oneDNN stream/engine integration). No deterministic setting or Torch deterministic-control bridge exists in that directory.
- `csrc/xpu/torch_bindings.cpp:47` registers `int4_gemm_w4a16`; `onednn/onednn_matmul.cpp:257` directly invokes the header implementation. Its attribute callback sets scratchpad, scales, zero points and math mode, without determinism. `onednn_ext.h:800–815` caches primitives by shape/strides/bias/grouping and invokes that callback on cache miss.
- Both kernel bases pin oneDNN `0e2a5bfeef1bfbffc3137464606540233086ce9b`; `FindoneDNN.cmake` is unchanged and packaging `nix/mk-kernels.nix` agrees. Torch 2.13/oneAPI 2026.0 remain the [selected dependency contract](../pair-compatibility.md). There is no dependency replacement covering the gap.
- Companion `compressed_tensors_wNa16.py:create_weights` selects the mixed-precision kernel; `kernels/linear/__init__.py:519` retains XPU W4A8 and `XPUwNa16LinearKernel`. The latter's `mixed_precision/xpu.py:100` calls this operation. Upstream `2c9d68f80bd20af7af1bc539c044a4aebbe52f8e` removes GPTQ activation ordering while retaining the external `g_idx` slot as `None`; `d4ee7fe7a98b3012f64309e18dd425e565069a01` adds backend selection. Neither changes this accumulation policy.
- W4A8 quantizes activations and is not an equivalent W4A16 path. INC's optional ARK backend belongs to its separate quantization route; its oneDNN fallback calls the same op and does not replace compressed-tensors dispatch. The [native E=1 experiment](../../../../benchmark-results/issue10-grouped-int4-20260909/REPORT.md) rejected integration: gate/up gains reversed across starts and down was 21–39% slower. Those historical results do not qualify a replacement here. Upstream `tests/test_int4_gemm_onednn.py` tests FP16 reference accuracy, without the BF16 replay requirement.

No prerequisite local patch, fold, schema, weight-layout or cross-repo ABI change is needed. RMSNorm/GDN/KVarN fixes remain independent. Keep the constant policy: introducing a mutable deterministic flag would require cache-key separation and policy-transition tests, beyond this minimal release change.

Observed validation is read-only commit/hash, tree/diff, dependency, dispatch and test inspection. No builds, GPU tests, source/ref/lock/service changes were performed. Historical evidence retained in the prior audit includes six passing W4 batch-separability cases and exact oneDNN replay of captured projections; these are not candidate qualification.

Before release, run the nine replay and six separability cases on the built candidate/B70, existing INT4 reference tests plus a production BF16 reference case, and fresh-process target/MTP2/vision replay. Record loaded extension, oneDNN implementation, driver and dependency identities. A controlled upstream-alone versus upstream+k01 comparison should use identical tensors and handle/allocation interventions; the patched arm must replay exactly, although the unpatched arm need not fail every shape. Compare released and candidate kernel latency plus end-to-end prefill/decode throughput under identical deterministic settings, workloads and cache configuration. An optional matched on/off experiment can measure policy cost; no such cost or new GPU pass is established here.
