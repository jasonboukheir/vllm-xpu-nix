# k07 — ADAPT native FlashAttention eager replay coverage

**Recommendation: ADAPT.** Preserve the nonduplicated repeatability requirement, strengthen native-dispatch and accuracy assertions, and consolidate repetitive setup. No production kernel change is justified by this test-only commit. Dropping its coverage is unsupported.

| Identity | Frozen value |
| --- | --- |
| Assigned commit | `8900abc2df3e6e85a64bef53b8465f313330c77a` |
| Parent | `be4d79b04ab65a32e6502a3ca0e10943751607d4` |
| Patch ID | `3c8305e2a5d37c0200dac3c0d55bc349206accf2` |
| Old kernel base / main | `e7f20cbc18d741419a84aa57178e1f1cc0b453da` / `9bb1e2d428b8a25b262c0cd6704614bdf6ee3ad8` |
| Released kernel tip | `2c39287deec5e59cc2656e6d4a6dce51a567c411` |
| Selected kernels / vLLM | `da16a5595c105605bcd44b558c7933b934a05f85` / `e378275a8f20eac9e92212e4a1fee84ec5a31dc7` |
| Companion old base / released tip | `51da0ca66c8065619c79e35dff97aa99aeaf5644` / `2fdd9d71f6895379b0940a8f755b4c9095840558` |

## Requirement and effective old stack

The parent-to-commit diff adds only the 210-line `tests/flash_attn/test_flash_attn_determinism.py`. Three dense causal prefill lengths, 104/107/162, plus paged prefill and single-query decode at KV length 162 give five cases. All use BF16, D256, 24 query heads, four KV heads and, when paged, 64-token pages. Each saves target output, alternates unrelated same-shape attention, retains differently sized allocator pressure across eight repetitions, checks equality, and empties the allocator cache. This guards dependence on previous calls, pointers or uninitialized working memory in the native attention used on Brutus/B70.

The test blob is identically `e2f13f537b566cdf0db61ceff85d22d682f48b96` at this commit and the released tip; subsequent local commits neither repair nor supersede it. The commit provides diagnostic coverage, not a demonstrated native-kernel fix. No fail-to-pass artifact for these exact five cases was located. Its contract is eager, fixed-input/fixed-shape replay, not graph replay, cross-batch invariance or fresh-service reproducibility.

## Upstream semantics and alternate coverage

At the selected kernel SHA, `flash_attn_interface.py:flash_attn_varlen_func` still calls `torch.ops._vllm_fa2_C.varlen_fwd`. `csrc/flash_attn/flash_api.cpp:mha_varlen_fwd` sends dense input through chunk prefill, paged multi-query input through masked prefill/decode dispatch, and qlen=1 through paged decode. These are distinct relevant paths. The original decode case has only three KV tiles: `get_num_splits` returns one below 16 tiles, so it does **not** cover split-K reduction scratch. The small uniform speculative-query redirect `_spec_decode_varlen_fwd` is also outside these five cases.

The old-to-selected range contains no changes under `tests/flash_attn`. Existing `test_flash_attn_varlen_func.py` covers dense/paged accuracy, strides, interleaved caches, windows, LSE, speculative queries and split-K, principally with `torch.testing.assert_close` against `ref_paged_attn`. It supplies a reusable oracle, but no equivalent repeated target/decoy/allocator sequence with exact comparison. Its latest change is already-old-base `571b9d9e80a9d5c47551d8a5b5df7e2a128deaaf` (#556). MLA decode tests exercise different geometry and operators.

Relevant newer production changes are `0b647c5717a382c6d7ea645571d3dbe8c7640696` (#574, general page-size dispatch), `08bf8ed99bdf2c41bc9ccd7393c56b4adc3e5b4a` (#581, Gemma configuration coverage), `5e5161efbfaa18b7d910650b7c8731d444c5359c` (#589, compilation), and `82542f4b74ce8c8ad81207075f5d2992b9754ea1` (#587, model configuration). None supplies replay coverage; #574 preserves the tested B64 policy. No selected-range changes affect the shared Xe2 prefill mainloop/epilogue or paged decode kernel files inspected.

Frozen vLLM still routes XPU through `vllm/v1/attention/backends/fa_utils.py` → `vllm/_xpu_ops.py:flash_attn_varlen_func` → this native wrapper. Its `tests/kernels/attention/test_flash_attn.py` explicitly uses CUDA; its batch-invariance suite tests a different contract, and MMEncoderAttention test device enumeration lacks XPU. These do not replace the local tests.

## Necessary adaptation and historical evidence

The caplog guard is unsound: upstream `81f1a27caa5344a95ce11d7ff59fdfc7da75fd01` (#555), present in both bases, warns only once per error via `_warned_missing_configs`, then calls `_fallback_varlen_attn`. Earlier tests can suppress the warning, allowing every replay to use reference attention unnoticed. Patch that helper to fail within a scoped fixture; preserve normal production fallback behavior. Assert finite baseline output and independently verify it using the existing reference before repetition. Compare integer/byte views if retaining “bitwise” terminology: `torch.equal` alone treats signed zeros as equal.

The saved `benchmark-results/kvarn/perf-ideas-20260908/issue6-body.md`, K7, independently identifies the warning-cache and missing-oracle gaps. Later `benchmark-results/pure-prefill-serving-20260909/wrapper-gates-summary.json` records eight native reference/replay passes, but also a repeatable reference-fallback output with four values outside unchanged tolerances. That directly demonstrates why repeatability alone is insufficient. Those measurements concern another immutable binary, not this candidate.

The same directory's `actual-prefill-replay/REVIEW.md` records 48 successful real-operand target-layer replays, including image inputs. Its `qualification-auto-baseline-review.md` nevertheless records only 10/16 exact output-token matches across unchanged MTP2 control starts. Instrumented native replay did not resolve that serving failure. Preserve both findings; neither proves this candidate qualified nor renders routine native regression tests redundant.

## Minimal residual, dependencies and validation

Keep one focused test change, preferably extending the surrounding attention suite and reusing its oracle. Preserve dense, paged-prefill and decode routes, interleaving and live allocator pressure. Retain the original lengths/repetitions until measured runtime and coverage justify reduction; removing duplicate setup needs no expanded Cartesian matrix. No ABI/schema dependency or production scratch-zeroing workaround is needed.

Kernel `9bb1e2d428b8a25b262c0cd6704614bdf6ee3ad8` changes shared attention templates while adding KVarN. Its compressed-cache replay tests are complementary, so run ordinary FA coverage after that feature and release-only repairs are folded. Companion `fcab69494f1719e91e416aace17ef6ad501fe900` supplies KVarN; released `KVarNAttentionImpl._prefill_first_chunk` and `_cached_multiquery_path` still use ordinary dense FA over raw/materialized KV. Compact support `80d5166fcb0b7935f3f95b8d343e3e669940261f` does not retire this path. Keep k07 independently identifiable rather than treating it as disposable feature scaffolding.

Observed here: exact patch/blob comparison, frozen source/history/dispatch inspection, and historical artifact review. No GPU, build, service or performance tests ran. Pending: execute the adapted cases on the rebuilt B70 candidate; verify forced fallback fails even with a populated warning cache; run reference plus exact replay checks, and relevant upstream split-K/window/stride cases. Record loaded DSO identity and CI duration. V2 DFlash2 D128/Q32/KV8 noncausal sliding-window K4V2 target/draft lifecycle, MTP rollback and vision require separate paired qualification; these causal D256 tests cannot certify them. Small BF16 reference tensors impose no intermediate BF16 serving milestone.
