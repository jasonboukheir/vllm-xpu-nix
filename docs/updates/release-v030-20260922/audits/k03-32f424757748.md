# k03: retain stable causal-convolution prefill

**Disposition: KEEP.** Retain the dispatch guard and focused replay regression as one independent correctness fix. The selected release supplies no replacement for the guarded convolution path. No follow-up repair needs folding into k03.

**Inputs and scope.** Read-only audit for `release-v030-20260922`; read the manifest, complete commit inventory, commit-audit procedure, and both source `AGENTS.md` files. Preserve the existing eager V1 dense-Qwen W4A16, K4V2 target/draft MTP2 and vision workload. Issue17 remains paused; this report proposes no V2/DFlash work.

| Identity | Frozen value |
| --- | --- |
| Commit | `32f42475774852d7174fe6cd9f8201878603381f` |
| Verified parent | `27a9c28f8598a531d34bcd92654ee55c291211c5` |
| Recomputed stable patch ID | `f7fd14e5d76964b1426c46ae21f46913f227194d` |
| Kernel old base | `e7f20cbc18d741419a84aa57178e1f1cc0b453da` |
| Kernel main / released tip | `9bb1e2d428b8a25b262c0cd6704614bdf6ee3ad8` / `2c39287deec5e59cc2656e6d4a6dce51a567c411` |
| Kernel v0.1.15 target | `1c7cbeee1cd0c1481d48f5031f679a47f3f0ef45` |
| vLLM old base | `51da0ca66c8065619c79e35dff97aa99aeaf5644` |
| vLLM main / released tip | `3b6c822e8a4f56dc3904c7fce388a76dff4c7bdd` / `2fdd9d71f6895379b0940a8f755b4c9095840558` |
| vLLM v0.30.0 target | `ced6857afa0ea7b2e3f0846a62e1394e90f15607` |

Verified prior report `docs/updates/issue17-20260920-snapshot-audit/audits/k03-32f424757748.md` SHA-256: `835158e3cf45019376aacdc4ea3251b420636804413e7d418e7e538550134d5b`. Its KEEP conclusion survives independent inspection; its snapshot targets and #600 coverage do not describe this release pair.

**Requirement and effective change.** Fresh Qwen prefills on Arc Pro B70 with oneAPI 2026.0 must produce stable Q/K/V, gates and final convolution state independently of recycled cache contents. Historical commit `1437161b97dfd7a6799ce2b2da5ab53fdcf36a6f` reports corruption and repeated `!` output at the eight-token threshold after metadata experiment `54d39759cdbd3ea16f9d4736af173cdfa5ba4c90`; this audit verifies that history, not its asserted compiler root cause.

k03 makes `enable_tiled_prefill=false` inside `causal_conv1d_non_spec`, selecting `gdn::chunk_causal_conv1d_xe2` with fused L2 normalization whenever the Xe2 prefill branch runs. Decode-only/non-Xe2 dispatch and operator schemas remain unchanged. The guard and `tests/gdn_attn/test_causal_conv1d_replay.py` survive unchanged at the full released tip. The test uses BF16, 162 tokens, 16 key/48 value heads, D128, width four, fresh state and `reorder_input=True`; poisoning cache with 0, 3.25 and −1.75 must leave intermediates, z and selected final state bitwise identical. It tests replay stability, not independent-reference accuracy or tiled dispatch.

**Target coverage and alternatives.** `git log old_base..target -- csrc/xpu/gdn_attn` is empty; both revisions resolve that entire directory to tree `fa1496f9edb4f687f71b1a63d05ea24a9810a108`. At target `csrc/xpu/gdn_attn/gdn_attn_interface.cpp:429–526`, prefills select tiling at `non_spec_token >= gdn::conv1d_tile_size`; the threshold is eight. The tiled header still broadcasts item-zero metadata through SLM and barriers. Its blob is `21a73ccc3aa46da79a84b54c1cef58225848e5a6`; untiled blob is `ccb6006d8e5ebf02c3ee33fbafd093a3c086e0fc`. The untiled implementation independently computes request positions, zero-initializes convolution history, gates cached-history loads on `has_initial_state`, and stages multi-token final state separately. Neither implementation changed upstream in this interval.

The ratio-three z-tail repair `b74c75307e596cf126724fdf8d812ce481d9b932` (#399) already precedes the old base; it is not new coverage. Speculative repair `da16a5595c105605bcd44b558c7933b934a05f85` (#600) is absent from v0.1.15, confirmed by ancestry. It concerns k06 rather than convolution-prefill dispatch.

At the companion target, `QwenGatedDeltaNetAttention` selects `forward_xpu` (`qwen_gdn_linear_attn.py:403`), which calls `gdn_attention_core_xpu` at line 1004. Unchanged `_gdn_attention_core_xpu_impl` (`vllm/_xpu_ops.py:137–225`) calls `_xpu_C.gdn_attention`, reaching this convolution. The changed FlashInfer backend eligibility applies to CUDA; it does not bypass the XPU route. No frozen-target alternative establishes retirement of the fallback. Narrowing the guard by device or importing an experimental tiled optimization requires new qualifying evidence.

**Dependencies.** No new ABI or prerequisite commit. Keep separate decisions for k04 `09925af1f9cb439803a63aa8c8e6704cb2932082` (delta-stage barriers), k05 `9e2ac704b8627e2fd8d37c42f769e615cfc1f122` with v03 `7de1e1c5ec5f3005b0e46bb0ec323f5e5864876c` (mixed-batch decode arithmetic), and k06 `be4d79b04ab65a32e6502a3ca0e10943751607d4` (ragged speculative rollback). k05 still routes its prefill subgroup through this guard. Later KVarN changes do not supersede it.

**Validation.** Completed: immutable identity/hash verification, effective-release diff, target implementation/call-path inspection, ancestry checks and successful `git show --format= 32f4247 | git apply --check` against the clean target. Historical `benchmark-results/releases/xpu-v1.8.0/kernel-regressions.sh` selects this replay test; its log records 34 passed, 17 skipped, 99 deselected on B70/Torch 2.13. This is prior-stack evidence only. No build, GPU execution or service action occurred.

Pending release qualification: run the replay regression and mixed/state-carry checks on the newly built pair. Before narrowing/dropping the guard, require dispatch-proven tiled/untiled comparisons against an independent convolution reference at 7/8/9, 162 and chunk-boundary lengths, fresh/dirty state, ratio-three z, both layouts and supported dtypes. Distinguish exact within-route replay from cross-route numerical tolerance; qualify public-model correctness and matched TTFT, including MTP/mixed scheduling.
