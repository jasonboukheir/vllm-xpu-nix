# k02 — disabled Xe2 chunk-prefill policy pruning

**Provisional KEEP; final retention requires measured compile evidence.** The release target still expands disabled host-dispatch trees. Upstream #589 repairs enabled device-template extern coverage and does not replace k02. Neither a current native build failure without k02 nor material time/RSS savings has been demonstrated. Preserve the requirement while resolving that gate; DROP is justified if controlled measurements establish immaterial benefit.

Auditor: `/root/r30_k02`. Scope: ordinary V1 K4V2/MTP2/vision release and existing narrow builds; issue17 remains paused, with no DFlash/D128 expansion or V2 work.

| Immutable input | Value |
| --- | --- |
| Commit / parent | `27a9c28f8598a531d34bcd92654ee55c291211c5` / `8530de9d4298b199436079c46233acb33efb6fc2` |
| Subject / stable patch ID | `fix(attn): avoid instantiating disabled chunk-prefill policies` / `a67e39bf2d719c6ffb4a8a0389b3e9d3bfbb6a76` |
| Kernel old base / rolling tip / released tip | `e7f20cbc18d741419a84aa57178e1f1cc0b453da` / `9bb1e2d428b8a25b262c0cd6704614bdf6ee3ad8` / `2c39287deec5e59cc2656e6d4a6dce51a567c411` |
| Kernel target | v0.1.15, `1c7cbeee1cd0c1481d48f5031f679a47f3f0ef45` |
| vLLM old base / rolling tip / released tip | `51da0ca66c8065619c79e35dff97aa99aeaf5644` / `3b6c822e8a4f56dc3904c7fce388a76dff4c7bdd` / `2fdd9d71f6895379b0940a8f755b4c9095840558` |
| vLLM target | v0.30.0, `ced6857afa0ea7b2e3f0846a62e1394e90f15607` |
| Packaging baseline | `4ac4375aa3d0f649fb4b6b43d9447f413a8b1cc7` |
| Prior report | `docs/updates/issue17-20260920-snapshot-audit/audits/k02-27a9c28f8598.md` |
| Verified prior SHA-256 | `f67e6ec1fc16ca9988d52c7de8f6c371704d0e1c51154f77483fd448316141cc` |

Read this run's manifest/map, complete paired commit inventories, commit-audit contract, both source AGENTS files, and prior report. Sources inspected in `build-dev/release-v030-20260922/{vllm,vllm-xpu-kernels}`; this report is the only written artifact.

The original claim was that narrowed builds needed protection from unsupported policy instantiation. More precisely, the old base already used leaf `if constexpr` policy/tuple guards to exclude disabled **device** implementations. Five runtime booleans nevertheless recursively expanded host `policy_dispatch_func` specializations before those checks. k02 adds `dispatch_enabled_chunk_policy`, extracts the same disabled-policy diagnostic, and redirects all twelve standard/b16 entry calls through an early policy guard. Enabled arithmetic and tuple checks remain intact. Both touched files are unchanged from k02 through the released tip; k03–k11 neither supersede nor repair it.

At the exact target, `csrc/xpu/attn/xe_2/chunk_prefill_utils.hpp:42–147` retains that original leaf gating and unguarded recursion. `chunk_prefill_configure.cmake:191–200,236–321` generates standard/b16 implementations, extern lists and availability traits only for configured policies/tuples. This generator and the trait template are unchanged from the old base. Alternate generated and fallback extern paths in `fmha_xe2.cpp:3–8` both declare `policy_dispatch_impl`, not the recursive host dispatcher.

`5e5161efbfaa18b7d910650b7c8731d444c5359c` (#589) changes `chunk_prefill_extern.hpp.in:26–86` to include paged global LSE=true for either causal value: twenty valid combinations per enabled policy. It avoids unintended enabled-device instantiation in the dispatcher TU. Disabled policies have no generated extern-list entry, and their host recursion remains. No equivalent early-pruning replacement exists in the inspected old-to-target Xe2 changes.

`0b647c5717a382c6d7ea645571d3dbe8c7640696` (#574), `fmha_xe2.cpp:256–356`, expands paged block support to every positive multiple of sixteen. Preserve its selection: block 32 and multiples of 64 use standard policies; remaining valid sizes use b16. Retaining k02 must not restore the older block-size validation.

Two distinct profiles matter:

- Packaging `flake.nix:236–249` narrow validation: D96 nonpaged/noncausal vision plus D256 causal paged/nonpaged, all LSE=false. Four policies are enabled and eight disabled. The source tree has 31 recursive plus 32 leaf specializations per disabled policy; bypassing 504 such specializations is a structural count, not measured resource savings.
- Actual Brutus `/home/jasonbk/.config/nix/hosts/brutus/services/vllm-xpu/package.nix` uses BMG AOT, `chunk_prefill_default` plus four D256/D64 extras, and `paged_decode_default` plus four extras. SHA-256: `7ac223d83a815c55976851aa1181187ae81aa8d9ae17f0c5a1fabed9e5779b95`. The target default already enables all six heads/twelve policies: k02 prunes no disabled tree here. This is the operational build control, distinct from narrow validation.

Minimum residual: existing early guard, shared diagnostic and twelve call substitutions. Preserve upstream extern coverage, tuple guards, namespace/visibility and b16 routing. k01 is only the historical parent; there is no operator/schema/ABI or vLLM patch dependency. The benefit depends on packaging selecting wholly disabled policies. No dispatcher-splitting patches or new build options are needed.

Completed read-only checks: reproduced patch ID and prior hash; confirmed both HEADs and `refs/upstream-releases/*` equal the frozen targets; source worktrees clean; ancestry checks for k02→released tip and #589→target passed; both later-local file equality and old-to-target guard/generator equality checks passed; counted twelve default-enabled policies. No native builds, GPU tests or compile measurements ran. Historical approximately 78 GiB RSS (`docs/releases/xpu-v1.8.0.md:48`) is not a with/without-k02 comparison; `docs/build.md:24–27` must not be treated as measured causal evidence.

Pending gate: compare otherwise identical target-based candidates with/without only k02, both containing #589, on the two profiles above. Freeze toolchain/SYCL-TLA, BMG flags and job limits; disable/isolate caches; retain repeated cold wall time, peak compiler RSS, failures, object size and available instantiation counts for `fmha_xe2.cpp`, then complete attention-library build results. Preserve exact source/config hashes. Final KEEP requires a reproducible build failure avoided or worthwhile resource benefit; successful indistinguishable builds support DROP.

After the parent builds candidates, existing reference checks should cover narrow D96 vision/D256 attention, standard/b16 paths including block 48, an actual-profile enabled paged-LSE tuple, disabled-policy versus missing-tuple diagnostics including logical D512, and native library identity. Ordinary paired V1 K4V2/MTP2/vision qualification remains required. These correctness checks cannot substitute for the compile comparison.
